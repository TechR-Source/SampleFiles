package practice;

public class SeleniumManagerDef {
	
	
	/* 
	  Selenium Manager is the Command line interface (CLI) tool to allow the cross platform execution. 
	  Whenever the execution flow is not identified the browser drivers, selenium manager will invoke the selenium bindings 
	  to execute the script in required browser. 
	  
	  version 4.6.0 onwards 
	  
	 */
	
}
